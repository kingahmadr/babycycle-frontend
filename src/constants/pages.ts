export const PAGE_LANDING = '/';
export const PAGE_LOGIN = '/login';
export const PAGE_REGISTER = '/register';
export const PAGE_LISTING = '/product';

// Explicitly define the array types as string[]
export const PUBLIC_ROUTES: string[] = [PAGE_LANDING, PAGE_LOGIN, PAGE_REGISTER];
export const PROTECTED_ROUTES: string[] = [];

